/**
 * Функция multiplyTwoNumbers должна возвращать (return) результат умножения двух чисел, переданные в неё первым и вторым аргументами
 * */

const multiplyTwoNumbers = (a, b) => {
};

multiplyTwoNumbers(3, 5); // Возвращает 15

module.exports = { multiplyTwoNumbers };
