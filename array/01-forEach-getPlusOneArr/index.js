/**
 * Дан массив целых чисел arr.
 * Функция getPlusOneArr должна возвращать (return) новый массив, в котором каждый элемент на 1 больше от исходоного.
 *
 * Использовать Array.prototype.forEach()
 *
 * Может понадобиться Array.prototype.push()
 * */

const arr = [1, 5, -1, 3, 0, -3, 7];

const getPlusOneArr = () => {
};

getPlusOneArr(arr); // Возвращает [2, 6, 0, 4, 1, -2, 8]

module.exports = { arr, getPlusOneArr };
